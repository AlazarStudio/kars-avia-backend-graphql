// Общий монотонный счётчик наблюдаемых событий. Двойник prisma и шпион pubsub
// пишут в свои журналы независимо, а часть характеризационных тестов проверяет
// порядок событий МЕЖДУ ними — например notify относительно publish.

let counter = 0

export function nextSeq() {
  counter += 1
  return counter
}
