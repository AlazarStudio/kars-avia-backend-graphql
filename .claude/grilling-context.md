# Контекст для grilling: kars-avia-backend-graphql

Читается скиллом `~/.claude/skills/grilling/SKILL.md` на шаге разведки.

Общая механика графа и ловушки поиска — `~/.claude/skills/grilling/references/graph-and-search.md`.
Ниже только отличия этого репозитория.

Здесь **только** механика добычи фактов и реестр источников решений. Домен описан в `CLAUDE.md`
этого репозитория и здесь не дублируется.

## 1. Где искать уже принятое

| Источник | Путь | Оговорка |
|---|---|---|
| Спеки и планы | `C:\github\kars-avia\docs\superpowers\specs\`, `...\plans\` | **Собственных спек у бэка нет** — решения по бэку живут в спеках фронта. Каталог в `.gitignore` фронта, грепать явно по пути |
| `rationale`-узлы графа | `graphify-out/graph.json` | Семантика доков прогонялась отдельным заходом |
| Память сессий | `~/.claude/projects/C--github-kars-avia/memory/` | Память общая на оба репозитория |
| Инструкции проекта | `CLAUDE.md`, `README.md`, `INSTALL.md` в корне | |

## 2. Особенности этого репозитория

- Интерпретер `graphify-out/.graphify_python` **в этом репозитории изначально не было** —
  если отсутствует, создать его с путём uv-интерпретера (см. общий файл), иначе команды
  скилла падают.
- Графом не покрыт `schema.prisma` — модели данных смотреть только чтением файла.
- Граф отдельный от фронта, кросс-репо рёбер нет: связь «резолвер ↔ вызывающий его запрос
  фронта» графом не прослеживается, только грепом по имени операции.
- В пути репозитория есть пробел (`C:\git back\...`) — в командах оболочки кавычить.
